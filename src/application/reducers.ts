import { playlistReducer } from "../components/playlist/state";
//import { listenReducer } from "../components/listen/state";

const reducers = {
  playlists: playlistReducer,
  // listen: listenReducer,
};

export default reducers;
